#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_mqtt@2.13.1@mqtt/mqtt.js" "$@"
  ret=$?
else 
  node  "$basedir/../_mqtt@2.13.1@mqtt/mqtt.js" "$@"
  ret=$?
fi
exit $ret
